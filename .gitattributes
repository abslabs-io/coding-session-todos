# Normalize line endings to LF in the repo and in every working tree,
# regardless of each contributor's core.autocrlf. Keeps Prettier
# (endOfLine: lf) and the shell hooks working on Windows/WSL.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.vsix binary
